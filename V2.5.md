# 艦これ Safari Safety v2.5

## 重要: 「規約違反にならない」とは保証できません

本ツールは非公式の第三者ツールです。過去の艦これ運営発言では、専用ブラウザやアドオンについて「厳密には規約違反」「規約には抵触してしまうかも」とされた記録があります。一方、運営が特に厳しく対処すると繰り返し明示しているのはマクロ/BOT/チート等の不正な自動操作です。

したがって v2.5 は「絶対に規約に抵触しない」と称しません。代わりに、技術的な危険要因をできるだけ減らすため、以下を**設計上の禁止事項**にしています。

- ゲームサーバーへツール独自の HTTP / XHR / fetch を送信しない
- APIの再送・リプレイをしない
- ゲームのリクエスト/レスポンスを書き換えない
- 自動出撃・自動進撃・自動撤退・自動遠征・自動任務巡回をしない
- `api_token`、Cookie、ログイン情報を保存・出力しない
- request body を保存・出力しない
- ゲーム操作を代行しない
- スクリーンショット、JSON書き出しは端末内でユーザーがボタンを押した時だけ実行する

利用可否の最終判断は、最新のDMM/艦これの規約・告知を確認してください。

## v2.5 構成

Safari / iPhone / iPad Userscripts では次の2ファイルだけを有効にします。

1. `kancolle-page-bridge.user.js` — ゲームframe内の通常通信を受動観測
2. `kancolle-safety.user.js` — DMM側UI、艦隊表示、大破ブロッカー、DB出力、スクショ

旧 `kancolle-audit-export.user.js` は v2.5 で no-op にしました。二重に XHR/fetch をフックしないためです。

## 大破安全機能

- 戦闘APIから戦闘後HPを計算
- HP 25%以下を大破として扱う
- 応急修理要員 / 応急修理女神は、艦の通常スロットと補強増設 (`api_slot_ex`) の両方を確認
- 大破艦すべてでダメコンを**確認できた場合**: ブロッカーを出さず、右上表示を黄色 `⚠ 大破(ダメコン)`
- ダメコンなし、装備照合不能、HP判定不能: 赤白点滅 `🚨 大破` + ブロッカー
- ダメコン装備大破のまま次戦闘へ進んだ後は、消費有無をサーバーの新しい艦データで再確認できるまで安全側で「未確認」に戻す

### ブロッカー

左端は常に `0px`、つまりブラウザ画面端です。ゲームの拡大率やiframeの余白に関係なく左側に隙間を作りません。

設定画面で次を調整できます。

- 右端
- 上端
- 下端
- 3回タップによる5秒解除の有効/無効
- 3秒の範囲プレビュー

## 艦隊表示

出撃艦隊について以下を表示します。

- 艦名 / Lv
- HP / 損傷状態
- Cond
- 燃料 / 最大燃料 / 残量率
- 弾薬 / 最大弾薬 / 残量率
- 弾薬残量から計算した与ダメージ補正の目安
- 燃料残量60%未満の警告
- 応急修理要員 / 応急修理女神の確認状態

燃料・弾薬は**最後にゲームサーバーから通常通信で受信した艦データ**を表示します。出撃中の現在値を推測で偽装しません。

## かんこれDB

`💾書き出し` を押すと、端末内に次の名前でJSONを保存します。

`かんこれDB_YYYY-MM-DD_HHMMSS.json`

収録対象:

- start2 マスターデータ
- 提督基本情報 / Lv / 経験値等
- 全所持艦（取得済み範囲）
- Lv / Exp / HP / Cond / 燃料 / 弾薬 / 装備スロット / 増設等の艦データ
- 全所持装備（取得済み範囲）
- 全艦隊 / 連合艦隊状態
- 燃料 / 弾薬 / 鋼材 / ボーキ
- バケツ / バーナー / 開発資材 / ネジ等
- 使用アイテム
- 入渠ドック
- 建造ドック
- 遠征 / ミッション
- 任務（手動表示した全ページを統合）
- 海域情報
- 基地航空隊（表示した方面を方面ID+基地IDで統合）
- 基地航空隊拡張情報
- 出撃マップ / ノード遷移
- 戦闘レスポンス / 戦闘結果（当該セッション）
- 出撃時艦隊スナップショット
- セッション中に通常取得できた `api_get_member/*` の生レスポンス
- 各APIの最終受信時刻

出力しないもの:

- `api_token`
- Cookie
- ログイン情報
- request body

## 「完全取得」の意味

v2.5はサーバーへ追加APIを送らないため、ゲーム本体がそのセッションで取得しなかった情報を強制取得することはできません。

取得状況画面を見ながら、通常操作で次を一度ずつ開いてください。

1. スクリプト有効状態で艦これを再読み込み
2. 母港
3. 編成
4. 改装 / 装備一覧
5. 入渠
6. 工廠
7. 遠征
8. 任務を1ページ目から最終ページまで
9. 出撃の海域選択
10. 基地航空隊がある各方面を順番に表示
11. 必要なら1回通常出撃して出撃/戦闘ログ取得を確認
12. 母港へ帰還
13. 取得状況を確認して `💾書き出し`

基地航空隊は最後に見た1方面で上書きせず、方面ID + 基地IDでセッション中に統合します。

## スクリーンショット

`📷スクショ` はゲームframe内の最大Canvasを端末内でPNG化し、`かんこれSS_YYYY-MM-DD_HHMMSS.png` として保存します。外部サーバーへ画像をアップロードしません。

Canvasのブラウザ保護によりPNG化できない環境ではエラーを表示し、別方式で画面を取得しようとする外部通信や権限拡張は行いません。

## Safety invariants

v2.5のコードレビューでは、少なくとも次を確認してください。

- Bridge内に `new XMLHttpRequest()` がないこと
- Bridge内にツール起点の `fetch(...)` 呼び出しがないこと
- 元の `XMLHttpRequest.send` / `fetch` を各ゲーム通信につき1回だけ呼ぶこと
- request body のpostMessage/保存がないこと
- `api_token` / Cookie を扱うコードがないこと
- 自動クリックコードがないこと
- DB/スクショ保存はユーザー操作からのみ呼ばれること

仕様変更時は、安全性を優先して「判定不能 → 赤警告/ブロック」とする fail-closed 方針です。
